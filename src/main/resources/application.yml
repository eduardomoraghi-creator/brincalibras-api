server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/brincalibras
    username: postgres
    password: "Naruto7769gu*$"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true